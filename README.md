# Collect and analyze data with  GitHub Actions usage


* Find GitHub repositories that have GitHub Action configured.
* Load the GitHub config file in a database
* Provide web interface to query vbarious aspects of the GitHub Actions. (eg. what triggers are used, what actions are used etc.)


* Start with a few fixed repositories
* Monitor the most popular repositories in GitHub?
* Can we use the GitHub API to find more repositories that have .github/workflows/ directory ?
